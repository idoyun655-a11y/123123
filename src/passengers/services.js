export const IMMIGRATION_STATUSES = Object.freeze({ OPEN: 'OPEN', REDUCED: 'REDUCED', CLOSED: 'CLOSED' });
export class CheckInZone { constructor({ id, terminalId, counters = 0, capacityPerHour = null, serviceTime = null, operatingStatus = 'OPERATIONAL' }) { Object.assign(this, { id, terminalId, counters, capacityPerHour, serviceTime, operatingStatus }); this.currentQueue = 'CHECK_IN_QUEUE'; } }
export class SecurityZone { constructor({ id, terminalId, lanes = 0, capacity = null, serviceTime = null, operatingStatus = 'OPERATIONAL' }) { Object.assign(this, { id, terminalId, lanes, capacity, serviceTime, operatingStatus }); this.queue = 'SECURITY_QUEUE'; } }
export class ImmigrationZone { constructor({ id, type, lanes = 0, capacity = null, serviceTime = null, status = IMMIGRATION_STATUSES.OPEN }) { if (!Object.values(IMMIGRATION_STATUSES).includes(status)) throw new RangeError(`Invalid immigration status: ${status}`); Object.assign(this, { id, type, lanes, capacity, serviceTime, status }); this.queue = type === 'ARRIVAL' ? 'ARRIVAL_IMMIGRATION_QUEUE' : 'DEPARTURE_IMMIGRATION_QUEUE'; } }
